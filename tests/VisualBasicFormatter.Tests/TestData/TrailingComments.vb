Module Annotated

    Public Sub Emit() ' entry point
        Dim line As String = String.Format("{0} {1} {2} {3} {4}", currentCustomerName, currentOrderNumber, currentInvoiceTotal, currentDueDate, currentStatus) ' the long one
        Dim short1 = 1 ' short
    End Sub

End Module
